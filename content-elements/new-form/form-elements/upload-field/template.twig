{#
    Parameter Header
    Fallback order: explicit parameter -> default value.

    formFieldElementId: Root data-bsi-element ID for the upload field.
    formFieldElementPartId: data-bsi-element-part ID for the field wrapper.
    formFieldHelperTextPartId: data-bsi-element-part ID for helper text.
    formFieldErrorInvalidPartId: data-bsi-element-part ID for invalid feedback.
    formFieldLabelText: Label text displayed above the upload control.
    formFieldHelperText: Optional helper text displayed below the field.
    formFieldErrorInvalidText: Validation error text for invalid file input.
    customClasses: Additional CSS classes for the root wrapper.
#}

{% set formFieldElementId = elementId ?: 'upload-form-field-5674018a' %}
{% set formFieldElementPartId = elementPartId ?: 'form-field-part-c21742af' %}
{% set formFieldHelperTextPartId = formHelperTextPartId ?: 'form-field-helper-text-part-0a34ce3d' %}
{% set formFieldErrorInvalidPartId = errorInvalidPartId ?: 'form-field-part-error-invalid-6ca6d1fd' %}
{% set formFieldLabelText = labelText ?: bsi_cx_lorem(3) %}
{% set formFieldHelperText = infoText ?: "" %}
{% set formFieldErrorInvalidText = errorInvalidText ?: 'Der Wert, den Sie für dieses Feld eingegeben haben, ist ungültig.' %}
{% set customClasses = customClasses ?: "" %}

<div x-data="fileUpload" data-bsi-element="{{ formFieldElementId }}" class="bsi-element-{{ formFieldElementId }} form-element {{ customClasses }}" data-bsi-element-part="{{ formFieldElementPartId }}" data-bsi-form-field-fixed-type="file">
    <label for="file-upload-field" class="form-label">{{ formFieldLabelText }}</label>
    <div 
        class="file-upload-container form-field-layout"
        @dragover.prevent="onDragOver"
        @dragleave.prevent="onDragLeave"
        @drop.prevent="onDrop"
    >
        <div class="file-upload-and-error-text">
            <div
                class="file-upload form-field-input-wrapper"
                x-cloak
            >
                {% block input_field %}
                    <input
                        @change="onFileChange"
                        @change-after-drop="onFileChangeAfterDrop"
                        @after-change="formElementValidationOnChange"
                        id="file-upload-field"
                        type="file"
                        x-ref="fileInput"
                        class="native-file-upload-input hiddenElement"
                    >
                    </input>

                    <div class="left">
                        <button
                            type="button"
                            class="upload-button"
                            @click="triggerFileUpload"
                            x-show="noFiles"
                            x-ref="uploadButton"
                            aria-label="Datei auswählen"
                        >
                            <span>Datei auswählen</span>
                            <i class="icon-upload"></i>
                        </button>
                        <div
                            class="file-info"
                            aria-live="polite"
                            x-show="hasFiles"
                        >
                            <img 
                                class="preview-image "
                                x-show="hasPreview"
                                :src="filePreview"
                                :alt="fileName"
                            />
                            <div class="file-text-wrapper">
                                <span class="second-form-label file-name" x-text="fileName"></span>
                                <span class="second-form-label file-suffix" x-text="fileSuffix"></span>
                                <span class="second-form-label file-size" x-text="fileSize"></span>
                            </div>
                        </div>
                    </div>

                    <div class="right">
                        <label
                            class="drop-text second-form-label"
                            aria-hidden="true"
                            x-show="noFiles"
                            x-ref="dropText"
                        >oder hier ablegen</label>

                        <button
                            type="button"
                            class="remove-button"
                            aria-label="Datei entfernen"
                            @click="removeFile"
                            x-show="hasFiles"
                            x-ref="removeButton"
                        >
                            <i class="icon-delete"></i>
                        </button>
                    </div>
                {% endblock %}
            </div>
        </div>
        <div class="form-field-feedback-wrapper">
            {% block input_field_error_text %}
                <div data-bsi-element-part="{{ formFieldErrorInvalidPartId }}" class="bsi-invalid-feedback">
                    {{ formFieldErrorInvalidText }}
                </div>
            {% endblock %}
        </div>
        
        <div class="helper-text-container">
            <div data-bsi-element-part="{{ formFieldHelperTextPartId }}" class="helper-text">{{ formFieldHelperText }}</div>
        </div>
    </div>
</div>
